export interface StudioSettings {
  studioName: string;
  tagline: string;
  contactEmail: string;
  phone: string;
  currency: string;
  darkMode: boolean;
  compactDensity: boolean;
  accent: string;
}
